org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Disable strict AAR metadata checking for Agora compatibility
android.useStrictIncompatibleSdkCheck=false
kotlin.jvm.target.validation.mode=warning
# Fix: Kotlin incremental compiler crashes on Windows when project (D:) and Pub cache (C:) are on different drives
kotlin.incremental=false
# Use file-system locking to avoid incremental cache corruption
org.gradle.daemon=false
